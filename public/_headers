# QA10 P1-2 修复：预览域 pages.dev 全站 noindex（静态路径）
#
# 背景：Cloudflare Pages 的 _headers 支持绝对 URL 规则（必须 https、不含端口），
# 可按 host 区分预览域与生产域。此规则仅匹配 palbreed-4ab.pages.dev 预览域，
# 生产域 palbreed.space 不匹配，保持 index,follow。
#
# 注意：
# 1. 每个规则只允许一个通配符（host 通配 + path 通配不能同时使用），
#    故此处使用具体预览域 + path 通配。
# 2. _headers 只作用于静态文件响应。v1.0.16 起无 on-demand Worker（/api/feedback
#    已删除，回归 mailto 方案），全站静态，本规则覆盖全部页面；middleware 仍
#    保留作为兜底（对 *.pages.dev 的 worker 响应设置同一 header）。
# 3. public/_routes.json（v1.0.2 手工添加，exclude 漏掉 299 个详情页）已删除，
#    改由 @astrojs/cloudflare adapter 自动生成。
https://palbreed-4ab.pages.dev/*
  X-Robots-Tag: noindex, nofollow
