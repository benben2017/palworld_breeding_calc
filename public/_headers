# QA10 P1-2 修复：预览域 pages.dev 全站 noindex（静态路径）
#
# 背景：Cloudflare Pages 的 _headers 支持绝对 URL 规则（必须 https、不含端口），
# 可按 host 区分预览域与生产域。此规则仅匹配 palbreed-4ab.pages.dev 预览域，
# 生产域 palbreed.space 不匹配，保持 index,follow。
#
# 注意：
# 1. 每个规则只允许一个通配符（host 通配 + path 通配不能同时使用），
#    故此处使用具体预览域 + path 通配。
# 2. _headers 只作用于静态文件响应（_routes.json exclude 列表内路径），
#    走 Worker 的路径（/api/* 等 on-demand 路由，含 /api/feedback）
#    由 src/middleware.ts 按 host 后缀 .pages.dev 统一设置同一 header，
#    两层配合覆盖全部请求。
# 3. v1.0.15 起删除 public/_routes.json（v1.0.2 手工添加，exclude 漏掉 299 个
#    详情页），改由 @astrojs/cloudflare adapter 自动生成（/pals/*、/tools/* 等
#    通配覆盖全部静态路径），避免引入 Worker 后详情页被误路由到 SSR。
https://palbreed-4ab.pages.dev/*
  X-Robots-Tag: noindex, nofollow
